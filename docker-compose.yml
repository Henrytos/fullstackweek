version: "3"

services:
  postgres:
    image: postgres:latest
    container_name: fsw-v3-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5439:5432"
    volumes:
      - .env:/app/.env

volumes:
  .postgres-data:
